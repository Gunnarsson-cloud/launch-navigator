# Global Launch Navigator

An interactive Streamlit app to visualise and manage a launch journey.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

The default flow and example data are defined in `data/default_flow.json`.
